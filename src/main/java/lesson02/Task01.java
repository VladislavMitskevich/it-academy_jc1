package lesson02;

public class Task01 {
    public static void main(String[] args) {
        int divisible = 9;
        int divisor = 5;
        System.out.printf("division result: %s / %s = %s\n", divisible, divisor, divisible/divisor);
        System.out.printf("the remainder of the division: %s / %s = %s", divisible, divisor, divisible%divisor);
    }
}
